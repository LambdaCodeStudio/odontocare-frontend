---
import LayoutCommon from "../app/LayoutCommon.astro";
---

<LayoutCommon>
	<div class="flex flex-col items-center justify-center min-h-screen px-6 mx-auto dark:bg-gray-900">
  
  <!-- Panel de Usuarios de Prueba -->
  <div id="demoUsersPanel" class="w-full max-w-md mb-4 p-4 bg-blue-50 border border-blue-200 rounded-lg dark:bg-gray-700 dark:border-gray-600">
    <div class="flex items-center justify-between mb-3">
      <div class="flex items-center">
        <svg class="w-5 h-5 text-blue-600 dark:text-blue-400 mr-2" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
        </svg>
        <h3 class="text-sm font-semibold text-gray-800 dark:text-white">Usuarios de Prueba</h3>
      </div>
      <button type="button" id="closeDemoPanel" class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-300">
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
        </svg>
      </button>
    </div>
    
    <div class="space-y-2 text-sm">
      <div class="flex items-center justify-between p-2 bg-white rounded dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer demo-user" data-email="admin@odontoclinic.com" data-password="admin2024">
        <div>
          <span class="font-medium text-gray-700 dark:text-gray-200">Admin:</span>
          <span class="text-gray-600 dark:text-gray-400 ml-1">admin@odontoclinic.com</span>
        </div>
        <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
        </svg>
      </div>
      
      <div class="flex items-center justify-between p-2 bg-white rounded dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer demo-user" data-email="jrodriguez@odontoclinic.com" data-password="clinica2024">
        <div>
          <span class="font-medium text-gray-700 dark:text-gray-200">Odontólogo:</span>
          <span class="text-gray-600 dark:text-gray-400 ml-1">jrodriguez@odontoclinic.com</span>
        </div>
        <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
        </svg>
      </div>
      
      <div class="flex items-center justify-between p-2 bg-white rounded dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer demo-user" data-email="aperez@odontoclinic.com" data-password="secretaria2024">
        <div>
          <span class="font-medium text-gray-700 dark:text-gray-200">Secretaria:</span>
          <span class="text-gray-600 dark:text-gray-400 ml-1">aperez@odontoclinic.com</span>
        </div>
        <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
        </svg>
      </div>
    </div>
    
    <p class="mt-3 text-xs text-gray-500 dark:text-gray-400 text-center">
      Haz clic en cualquier usuario para autocompletar el formulario
    </p>
  </div>

  <!-- Formulario de Login Original -->
  <div class="w-full max-w-md p-8 space-y-3 rounded-xl bg-white dark:bg-gray-800">
    <h1 class="text-2xl font-bold text-center text-gray-700 dark:text-white">
      Iniciar sesión
    </h1>

    <form id="loginForm" class="space-y-6" novalidate>
      <div>
        <label for="email" class="block mb-2 text-sm font-medium text-gray-900 dark:text-gray-300">Correo electrónico</label>
        <input type="email" name="email" id="email" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white" placeholder="nombre@company.com" required>
        <p class="mt-2 text-sm text-red-600 dark:text-red-500 hidden" id="emailError">Por favor, introduce un correo electrónico válido.</p>
      </div>

      <div>
        <label for="password" class="block mb-2 text-sm font-medium text-gray-900 dark:text-gray-300">Contraseña</label>
        <input type="password" name="password" id="password" placeholder="••••••••" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-600 dark:border-gray-500 dark:placeholder-gray-400 dark:text-white" required>
        <p class="mt-2 text-sm text-red-600 dark:text-red-500 hidden" id="passwordError">Por favor, introduce la contraseña.</p>
      </div>

      <div class="flex items-start">
        <p class="text-lg text-red-500 dark:text-red-500 hidden" id="errorMessage" role="alert"></p>
      </div>

      <button type="submit" id="submitBtn" class="w-full text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
        <div class="flex justify-center items-center">
          <span>Iniciar sesión</span>
          <div role="status" id="loadingSpinner" class="hidden ml-3">
            <svg aria-hidden="true" class="w-4 h-4 text-gray-200 animate-spin dark:text-gray-600 fill-blue-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 100 101">
              <path d="M100 50.5908C100 78.2051 77.6142 100.591 50 100.591C22.3858 100.591 0 78.2051 0 50.5908C0 22.9766 22.3858 0.59082 50 0.59082C77.6142 0.59082 100 22.9766 100 50.5908ZM9.08144 50.5908C9.08144 73.1895 27.4013 91.5094 50 91.5094C72.5987 91.5094 90.9186 73.1895 90.9186 50.5908C90.9186 27.9921 72.5987 9.67226 50 9.67226C27.4013 9.67226 9.08144 27.9921 9.08144 50.5908Z" fill="currentColor"/>
              <path d="M93.9676 39.0409C96.393 38.4038 97.8624 35.9116 97.0079 33.5539C95.2932 28.8227 92.871 24.3692 89.8167 20.348C85.8452 15.1192 80.8826 10.7238 75.2124 7.41289C69.5422 4.10194 63.2754 1.94025 56.7698 1.05124C51.7666 0.367541 46.6976 0.446843 41.7345 1.27873C39.2613 1.69328 37.813 4.19778 38.4501 6.62326C39.0873 9.04874 41.5694 10.4717 44.0505 10.1071C47.8511 9.54855 51.7191 9.52689 55.5402 10.0491C60.8642 10.7766 65.9928 12.5457 70.6331 15.2552C75.2735 17.9648 79.3347 21.5619 82.5849 25.841C84.9175 28.9121 86.7997 32.2913 88.1811 35.8758C89.083 38.2158 91.5421 39.6781 93.9676 39.0409Z" fill="currentFill"/>
            </svg>
            <span class="sr-only">Cargando...</span>
          </div>
        </div>
      </button>
      

      <div class="text-sm font-medium text-gray-500 dark:text-gray-300">
        ¿Aún no tienes una cuenta? <a href="/logup" class="text-blue-700 hover:underline dark:text-blue-500">Regístrate</a>
        <span class="mx-2">|</span>
        <a href="/recuperar-contra" class="text-blue-700 hover:underline dark:text-blue-500">¿Olvidaste tu contraseña?</a>
      </div>
    </form>
  </div>
</main>
</LayoutCommon>

<script>
const BASE_URL = "";

//imports
import { setUserRole, setUserId } from '../auth/middleware'

// Obtener referencias a los elementos del DOM
const loginForm = document.getElementById('loginForm') as HTMLFormElement;
const emailInput = document.getElementById('email') as HTMLInputElement;
const passwordInput = document.getElementById('password') as HTMLInputElement;
const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement;
const loadingSpinner = document.getElementById('loadingSpinner') as HTMLDivElement;
const errorMessage = document.getElementById('errorMessage') as HTMLParagraphElement;

// Referencias para el panel de demo
const demoUsersPanel = document.getElementById('demoUsersPanel') as HTMLDivElement;
const closeDemoPanel = document.getElementById('closeDemoPanel') as HTMLButtonElement;
const demoUserElements = document.querySelectorAll('.demo-user');

// Funcionalidad del panel de demo
if (closeDemoPanel && demoUsersPanel) {
  closeDemoPanel.addEventListener('click', () => {
    demoUsersPanel.style.display = 'none';
  });
}

// Autocompletar formulario al hacer clic en un usuario demo
demoUserElements.forEach(element => {
  element.addEventListener('click', () => {
    const email = element.getAttribute('data-email');
    const password = element.getAttribute('data-password');
    
    if (email && password && emailInput && passwordInput) {
      emailInput.value = email;
      passwordInput.value = password;
      
      // Efecto visual de feedback
      emailInput.classList.add('ring-2', 'ring-blue-500');
      passwordInput.classList.add('ring-2', 'ring-blue-500');
      
      setTimeout(() => {
        emailInput.classList.remove('ring-2', 'ring-blue-500');
        passwordInput.classList.remove('ring-2', 'ring-blue-500');
      }, 1000);
    }
  });
});

interface LoginResponse {
  success: boolean;
  message: string;
  rol?: string;
  userId: string;  // Ya no es opcional porque sabemos que viene
}

const validateUser = async (email: string, password: string): Promise<LoginResponse> => {
  try {
    // 1. Primero hacemos el login
    const loginResponse = await fetch(`${BASE_URL}/login/login`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ email, password }),
    });

    if (!loginResponse.ok) {
      throw new Error('Error al iniciar sesión. Verifica tus credenciales.');
    }

    const loginData = await loginResponse.json();
    console.log('Respuesta del login:', loginData);

    // 2. Si el login es exitoso, buscamos el usuario según su rol
    if (loginData.success) {
      let userResponse;
      
      if (loginData.rol === 'odontologo') {
        userResponse = await fetch(`${BASE_URL}/medico/findByEmail/${email}`);
        const medicoData = await userResponse.json();
        
        if (medicoData.medico) {
          return {
            success: true,
            message: loginData.message,
            rol: loginData.rol,
            userId: medicoData.medico._id
          };
        }
      } 
      else if (loginData.rol === 'secretaria') {
        userResponse = await fetch(`${BASE_URL}/secretaria/buscar/${email}`);
        const secretariaData = await userResponse.json();
        
        if (secretariaData.secretaria) {
          return {
            success: true,
            message: loginData.message,
            rol: loginData.rol,
            userId: secretariaData.secretaria._id
          };
        }
      }
      else if (loginData.rol === 'admin') {
        // Para admin, usamos el email como ID temporal ya que no hay schema de admin
        return {
          success: true,
          message: loginData.message,
          rol: loginData.rol,
          userId: email // O podrías generar un ID único
        };
      }

      // Si no se encontró el usuario según su rol
      return {
        success: false,
        message: `No se encontró el perfil de ${loginData.rol}`,
        userId: '',
        rol: ''
      };
    }

    return {
      success: false,
      message: 'No se pudo verificar la identidad del usuario',
      userId: '',
      rol: ''
    };

  } catch (error) {
    console.error('Error en validateUser:', error);
    return {
      success: false,
      message: error instanceof Error ? error.message : 'Error en la autenticación',
      userId: '',
      rol: ''
    };
  }
};

// Event listener del formulario
if (loginForm && emailInput && passwordInput && submitBtn && loadingSpinner && errorMessage) {
  loginForm.addEventListener('submit', async (event) => {
    event.preventDefault();

    submitBtn.disabled = true;
    loadingSpinner.classList.remove('hidden');
    errorMessage.classList.add('hidden');

    const response = await validateUser(emailInput.value, passwordInput.value);
    
    console.log('Respuesta completa:', response); // Debug

    if (response.success && response.userId) {
      console.log('Setting user role:', response.rol); // Debug
      console.log('Setting user ID:', response.userId); // Debug
      setUserRole(response.rol);
      setUserId(response.userId);

      // Pequeño delay para asegurar que el localStorage se actualice
      setTimeout(() => {
        window.location.href = '/';
      }, 100);
    } else {
      errorMessage.textContent = response.message;
      errorMessage.classList.remove('hidden');
    }

    submitBtn.disabled = false;
    loadingSpinner.classList.add('hidden');
  });
}
</script>